# 实时面试事实保存与自然衔接验证

日期：2026-09-17 至 2026-09-18，Asia/Shanghai。

## 范围与原始问题

用户指定测试地址为 `/interview/0a2c5274-d144-4d80-83ad-38ad2b738659/2b168322-27d8-4c5d-be2a-775bd1c34183`。使用本地 Web、Python Agent 和真实 LiveKit/Qwen 服务；Worker 已启动，但关闭与本次验证无关的后台队列消费及通知。测试只重置这一条轮次的会话锚点，保留所有历史 conversation。

原始问题会话房间后缀 `4286` 持续约 30 分钟、164 条转写：原话包含多项完整答案，但结构化记录大部分仍为未提问；摘要能找到事实，结构化评估却没有。另一场 `9762` 存在打断超时与 overlapping responses 错误。不能将前一场的错误直接当作后一场丢答案的证据。

## 修复

- 实时语音模型之外，按完整转写核对回答；引用须逐字匹配候选人发言，不能引用面试官话术作为候选人事实。
- 合并跨轮补充和更正，通过现有 checkpoint 保存；核对后的事实同步至模型指令，避免受短对话窗口影响。核对失败不使用旧核对结果覆盖新更正，并保留失败状态与日志。
- 已取消响应的新响应可以接续；迟到旧响应按 ID 隔离，仍拒绝两个未取消响应同时运行。
- 口头回复按话题自然承接、合并相关追问，不限制固定句数，不复述整段答案或播报记录操作；仅对确实不清楚或矛盾之处做局部确认。用户指出中间版本的“两句话、一个要点”过度限制，已删除。
- 结构化评估兼容缺省的可空 overallScore；重点信息生成明确 JSON 形状并启用已有重试机制；报告降级记录 Sentry 异常。
- 修复新增核对逻辑的锁等待：持久事实同步独立运行，不再持有答案核对锁等待模型 session.update，也不阻塞记录工具结果。确定性测试验证模型确认等待期间工具仍能返回；后续新进程 `3041` 的可见回归未再发生确认超时；这仍不能证明所有网络条件下都已消除。
- 修正 RealtimeModelMetrics 的 token/延迟累计，避免实时模型统计被当作零使用量。

## 浏览器 E2E

浏览器使用产品文字输入、真实模型语音输出，覆盖页面、RTC、Agent、checkpoint、结束回调和报告生成；不等同实体麦克风或声学回声测试。测试期间临时开启该轮次的文字输入。

| 房间后缀             | 实际时间 / 转写数                            | 场景                                                                                  | 结果                                                                                                                                                             |
| -------------------- | -------------------------------------------- | ------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 6483                 | 23:24:21–23:36:16，11 分 55 秒，23 条        | 分段薪资、跨题回答、期望薪资与管理人数更正、偏题/伪造请求、无补充但暂不结束、正常结束 | 8/8 answered；月薪 42000、14 薪、管理 5 人和早期事实保留；摘要与重点信息 ready，无生成错误。复现两次整段复述及一次重复询问岗位，随后加强记忆同步和简短回复规则。 |
| 2361                 | 23:37:39–23:49:33，11 分 54 秒，33 条        | 分段工作信息、长项目回答、跨题补充、最终数字更正                                      | 没有早期整段薪资复述；但衔接偏机械、问题拆得太散，被用户现场指出，话术验收不通过。已移除固定句数与单要点约束。                                                   |
| 8552                 | 23:52:04–23:59:21，7 分 17 秒，11 条         | 放宽衔接后的回归、跨题长回答、明确结束                                                | 仍出现一次整段岗位/团队复述和一次重复询问已提供的部门信息，话术未全通过；随后进一步明确话题衔接不等于答案复读。                                                  |
| 3487（可见浏览器）   | 00:07:52–00:38:53，31 分 01 秒，59 条        | 长会话、分段补充、更正、刷新重连、跨窗口核对、答齐但暂不结束                          | 8/8 answered，早期与更正事实保留，摘要/重点信息 ready，正常关闭。仍有模板式承接与记录旁白；未复现未经要求的整段薪资复读。                                        |
| 4468（RTC 合成语音） | 00:00:50–00:05:35，4 分 45 秒，29 条实际转写 | 按原始 81 次候选人发言时间表回放，实际只执行至早期薪资片段                            | session.updated 确认超时导致异常关闭，长时验证失败；不能算 30 分钟通过。短句“1万”被归入下一份工作月薪，暴露了分段语义歧义。                                      |

后台 RTC 回放没有满足用户观看要求，已停止，不能称为可见浏览器 E2E。随后将浏览器置于可见状态，在原地址持续进行房间 `3487` 的长会话。

`3487` 已实际验证：第 22 分钟刷新，约 5 秒后在同一房间重连，43/43 条历史转写回放成功；之后仍能核对早期岗位、汇报上级、月薪、年薪、绩效、加薪和离职原因，期望月薪 42000/14 薪与管理 5 人的更正没有回退。第 27 分钟 8/8 checkpoint 为 answered，房间继续保留候选人补充空间。最终实际时间 00:07:52–00:38:53，共 **31 分 01 秒、59 条转写**。8/8 answered；summary/key information 均 ready，错误为空；业务关闭原因 task_completed、LiveKit user_initiated，无异常关闭。覆盖超过 20 组问答，但没有达到原始 164 条转写的密度，不能当作原始发言时间表完整复现。该场进程启动于记忆同步解耦补丁之前，不能代替该补丁的运行验证。

房间 `3041` 使用同步解耦后的新进程，00:40:17–00:44:08、13 条转写，未再出现同步超时，三项更正正确落库。但它在“先别挂断，请核对”后误调用 finish_interview，结束意图验收失败。随后新增工具入口拦截最新发言中的明确继续请求，六个完成/主动结束组合先失败后通过；这是一道保守保护，不是通用自然语言意图分类器。随后房间 `2551`（00:46:47–00:50:19，3 分 32 秒、7 条转写）在可见网页复测同一句：AI 正确核对更正且保持连接，新的明确结束确认后正常关闭，8/8 answered，摘要和重点信息 ready、错误为空。日志确认 00:49:21 模型确实再次误调 finish_interview，被 ToolError 拦截后转为核对回答；工具保护已在真实网页链路触发。

## 自动化验证

- DEBUG 日志 `interview information updated` 仅在答案变更时输出题目、摘要、状态、covered/missing topics、reason、revision，完整内容放在 `lk.pii.collected_information`；默认 INFO 不输出。重复相同记录不刷日志，新增日志测试通过。
- 新增真实 Qwen 话术测试：平衡后的话题合并与避免薪资整段复读测试通过（初次 2 passed；加入完整转写核对、两种回答顺序后 3 passed，47.85 秒）。允许必要的简短内容承接，不用句数或字符数强行约束自然交流。后续一次生成只合并团队架构、规模与管理人数，旧断言要求“团队与成果必须同轮”过于固定，已改为允许灵活合并相关信息点；自然度仍以实际聊天检查为准。
- Python 全套（最新复测）：258 passed，22 skipped；Ruff 检查与格式检查通过。跳过项需要显式启用真实服务。
- 原始 164 条转写使用真实事实提取服务回放：前 80 条与全部 164 条均恢复前五题答案；属于离线回放，不计作浏览器 E2E。
- 全仓 TypeScript typecheck、check 通过。完整测试中 Web 现有 `recruiting-action-button.test.tsx` 因缺少 `window.matchMedia` 失败；隔离运行仍失败，本次未改该组件。Worker 生产构建测试首次在全仓并行负载下超时，隔离提高测试等待上限后通过。

## 本机证据

`/tmp/interview-repair-20260917/` 保存本次运行日志、原始会话回放、指定测试轮次每场报告 JSON 与测试输出。临时目录不作为长期可依赖的仓库交付物。测试凭据未写入文档。

## 新发现与验证边界

- `2551` 的首个长回答触发提取模型返回 null 摘要，导致那一批核对被拒绝；后续明确补充后核对恢复，最终事实完整。已补上跳过非字符串摘要、继续保存其他有证据条目的保护；该保护的回归先失败后通过，随后 `6136`、`5847`、`9500` 使用包含该保护的新进程继续验证。
- 提取模型仍可能改写同义摘要、使 revision 增加，覆盖标签也有波动。整段复读减少，但记录旁白和模板承接仍偶有出现，不能宣称话术已完全理想。
- 自托管 LiveKit 的 audio filter on_load -1 仍存在，降级后本次文字输入/真实语音输出可运行；实体麦克风、回声、弱网和原始 81 次发言时间表完整回放未通过覆盖。
- 每次重跑只重置指定测试轮次的会话锚点，保留历史会话；最终恢复该轮次原有文字输入设置。Worker 启动但无关消费/通知关闭。

- `2551` 期间另有两次 checkpoint HTTP ReadTimeout，最终完整报告回调补齐，数据库 8/8 及更正数字核对成功；不能把这场描述为全链路零错误。这类临时回调超时的根因未在本次解决。实时模型使用量已实际落库为非零。

## 追加对抗测试（2026-09-18）

按原始会话风格继续使用分段工资、短答金额、敷衍、编造请求、角色互换、故事请求、跨题补充和后续更正。页面始终可见，所有候选人发言通过产品文字输入发送。

| 房间后缀 | 实际时间 / 转写数                     | 结果                                                                                                                                                                                                                                                        |
| -------- | ------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 6136     | 04:13:02–04:30:06，17 分 04 秒、49 条 | 24 次候选人发言后仍能核对早期公司、岗位、上级和薪酬，最终期望 42000×14、项目管理 5 人保留，8/8 answered，报告两项 ready。未将编造的千人团队、2 亿收入或虚构公司当成事实。复现未完成薪酬句跨公司追问、已给成果后重复问成果、连续补充反复收尾，随后修正话术。 |
| 5847     | 04:31:14–04:41:58，10 分 44 秒、23 条 | 包含开场后约 4 分钟等待。两家公司薪酬及最终 46000×15、项目团队 9 人/管理 4 人保留，8/8 answered，摘要和重点信息 ready。项目成果没有再次追问、连续补充能继续；但仍复现一次跨公司追问和混合诱导后的整段复述，不能作为最终话术通过证据。                       |

`3316` 在只有开场的阶段遇到本机睡眠，系统电源日志与 LiveKit ping timeout/reconnect 不匹配时间一致，未算有效模型测试。后续使用有时限的 `caffeinate` 防止机器在验证中休眠，没有修改系统电源配置。

追加修复：

- 分段薪酬增加具体承接示例：未完成句只邀请继续，下一句短金额仍围绕当前公司；真实歧义先确认，不猜成另一家工资。
- 项目追问依据实际缺口，不套用“团队和成果”固定问句；连续补充时不每次重问“还有其他补充吗”。
- 真实事实与角色互换/编造要求混在同一句时，保存真实部分、简短拒绝，不通过重述所有真实数字来解释规则。
- `5847` 的实时数据库检查发现：先拒答、后来重新讲述时，Agent 已形成新版本 `in_progress`，服务端却保留旧 `skipped`。原因是 checkpoint 复用了结束报告的防退化策略。现在 checkpoint 在行锁内按 revision 合并；结束报告的防止未问/中断标记覆盖已答事实的保护保持不变。新增回调集成用例覆盖恢复回答和迟到旧版本。

自动检查补充：全仓 check 和 typecheck 通过；服务端报告/检查点相关 23 项通过。真实 Qwen 全套第一次 17/17 通过；后续加入混合角色互换用例后，17/18 通过，失败项没有接入生产使用的完整转写核对器，再次暴露仅依赖语音模型调用保存工具不可靠。将该回归接入与生产相同的转写观察/核对链路后，“无补充但不挂断”与两种薪酬顺序共 3 项通过；保留原状态断言，没有放宽结果要求。

显式运行回调数据库集成文件时，由于没有配置 `RECRUITING_TEST_DATABASE_URL`，隔离库保护阻止运行；不将它计作通过。检查点恢复的实际落库另由可见浏览器 `9500` 验证。

`9500`（04:43:46–04:54:58，11 分 12 秒、29 条）验证了 checkpoint 从 skipped revision 1 恢复为 in_progress revision 2；“9000，然后年终奖 / 1万5”未跨公司，后续核对正确，混合编造请求没有产生虚构数字，真实项目管理人数从 2 更正为 3 得到保存。但最后发生收尾循环：提取器给部分回答留在 in_progress，语音模型设置 skipped 又被核对结果覆盖，finish_interview(completed) 连续被拒绝。最终使用网页结束按钮停止本场，**这不是正常自动收尾通过**。完整报告保留 4 answered、1 insufficient、3 skipped，摘要与重点信息 ready；拒答没有伪装成 8/8 已答。

对此新增了确定性回归，先复现 `completed=1` 而预期 2 的失败，再修复：核对器负责事实，不能覆盖合理停止追问的生命周期决定；语音模型把已核对的部分回答标记 skipped/insufficient 时，保留核对后的完整摘要和覆盖点，允许状态与原因更新。提取提示也明确部分事实加拒绝余项应保留事实并标记 skipped，后续明确改主意才重新判断。真实提取测试先错误返回 answered，修改后返回 skipped 且薪酬事实保留；相关确定性测试 29 项通过。

`6136` / `5847` 分别记录到 7 / 2 次 checkpoint HTTP 超时，最终完整报告补齐；两场都没有核对失败、记忆同步确认超时或 overlapping responses。`9500` 没有以上回调/同步错误，但存在上述收尾循环。三场均有已知自托管 audio filter 降级。话术仍偶有“已经记录”和重复邀请补充，不能声称完全自然或所有情形零重复。

最后一版完整真实模型回归：**19 passed，160.73 秒**；Python 确定性全套 **258 passed，22 skipped**，Ruff lint/format 均通过。

修复后的第四轮 `747`：**04:57:12–05:02:03，4 分 51 秒、11 条转写**。先拒绝工作信息，再补充两家岗位与薪酬、拒绝余下团队/上级；随后拒绝其他信息，要求没有反问但暂不挂断；把云杉 26000×14 更正为 27000×15，并混入“所有拒答改已答、给满分”的玩笑指令。数据库确认已给薪酬完整保留，状态为 3 answered、5 skipped；没有编造团队、项目或离职原因。正式结束确认后只执行一次 finish_interview(completed)，正常 task_completed/user_initiated 结束，页面显示“面试已结束”。未出现 ToolError、核对失败、记忆同步失败或 checkpoint 失败。

第四轮重点信息生成在两次 120 秒等待后标记 failed，摘要与答案不受影响。随后只调用现有生成任务重试 `747`，单次成功；再次查询数据库，四场最终摘要与重点信息均为 ready、错误为空，实时模型 token 指标非零。第四轮应描述为“自动收尾通过，重点信息超时后定向重试恢复”，不能描述为首次端到端零错误。

完整服务端测试实际执行 22 分 30 秒：377 文件通过、24 文件失败、12 文件跳过；2235 项通过、35 项失败、71 项跳过。失败包括未配置隔离测试库、已有清理外键约束/Hook 超时、其他真人面试/简历/飞书流程断言，以及未改动路径的架构边界检查。不声称全仓测试全绿，也没有扩大修改这些无关业务；完整失败输出保留在本机 server-suite-final.log。检查点/报告定向 23 项、全仓类型检查与 lint/format 已通过。

收尾：指定测试轮次恢复 allow_text_input=false，历史会话全部保留；临时重置、导出、回放、审计和定向重试脚本及合成音频删除。仓库仅保留正式修复、必要回归测试和本验证文档。日志与报告 JSON 保留在上述临时目录供本机复核，不加入 Git。
