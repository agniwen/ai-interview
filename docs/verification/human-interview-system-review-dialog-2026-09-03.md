# 系统内真人面试评价弹窗验证

日期：2026-09-03

## 本次范围

- 候选人详情的“评价并完成”打开系统弹窗，原会议链接评价入口保留。
- 共用评价表单及提交处理：保存草稿不关闭，提交要求通过或不通过，成功后关闭并刷新轮次。
- 关闭按钮、右上角关闭和未保存修改确认共用同一关闭处理。
- 新生成的 AI 评价待确认通知使用候选人详情深链接，携带 `reviewRoundId`，不再被收件人会议链接覆盖。
- 登录回跳保留具体候选人和轮次；系统接口同时验证工作区权限、招聘数据可见范围和实际面试官身份。
- 未修改真实候选人评价，未发送测试飞书通知，未重发历史消息。

## 页面验证

在现有 Chrome 登录会话中验证：

1. 在候选人详情点击“评价并完成”，读取指定轮次评价并打开页面内弹窗。
2. URL 保留候选人详情路径，增加 `tab=human-interview&reviewRoundId=...`。
3. 刷新该链接后自动打开相同轮次，不进入会议、不请求麦克风。
4. 关闭后留在候选人详情，仅清除弹窗参数。
5. 实际未登录接口请求返回 401。

## 自动化验证

- Web 定向回归：29 项通过（共用表单、真实 Dialog 关闭保护、查询参数、登录回跳）。
- Server 定向回归：34 项通过（系统/邀请接口、真实数据库访问范围、通知链接、原 AI 邀请通知）。
- 所有 TypeScript 包类型检查通过。
- 全仓格式和静态检查通过，存在共用表单关闭回调引起的 2 条 `react/refs` 非阻断警告；关闭保护已由事件测试验证。
- Web 全量 776 项通过，随后新增的 Dialog 关闭测试也在定向回归中通过。
- Worker 全量 149 项通过；Desktop 分批重跑 260 项通过（首次并行执行有 1 项旧音频压力测试超时）。
- Python：ruff 检查、格式检查通过，150 项测试通过。
- 独立后端 `createServerApp` 导入通过。

数据库测试使用本次创建的本地临时 PostgreSQL，未连接共享开发库。临时库补齐历史搜索触发器、技能计数触发器、阶段约束和通知模板后，相关失败项复测通过。

后端全量最后一轮为 1873 项通过、3 项失败、28 项跳过；其中 1 项由临时库缺少候选人异常邮件模板导致，补齐后整组 5 项复测通过。剩余 2 项是既有包边界断言：Worker 依赖 `@app/server`，以及 Server 暴露 `./web/document-sync`。本次未扩大范围调整这些依赖。

## 行为边界

- 已经发出的飞书卡片不会自动改写；后续新生成的评价待确认通知走系统入口。
- 系统入口需要登录且具备本轮访问和修改权限；原签名会议链接保持兼容。
- 本次不执行新的业务数据库迁移，不提交或重生成真实评价。
