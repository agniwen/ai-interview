# 统一列表筛选验证（2026-08-26）

基线：`aee04c5cdc8e823f15d9c212a7398bbc3267a781`。范围见 [ADR 0032](../adr/0032-replace-list-filtering-in-one-coordinated-release.md)。

## 自动检查

- `bun run check`：通过。
- `bun run typecheck`：8 个 workspace 通过；后续 Web、Desktop 增量类型检查通过。
- Web 全量测试：681 项通过；筛选转换、未完成条件、Apply/Cancel、原子分页重置及清除勾选均有回归测试。
- Desktop 全量测试：162 项通过。首次多 workspace 并发运行有录音流测试超时，串行 workspace 复跑通过。
- Python：ruff check、ruff format --check、146 项 pytest 均通过。
- 后端：1564 项通过、24 项跳过、6 项失败；另一个权限测试套件缺少认证环境配置，补充本地测试占位配置后单独通过。

后端 6 项失败均位于本次未修改的代码：

1. `job-description-match-agent.test.ts`：仍期待旧“岗位要求”提示文本。
2. `lifecycle-write-boundary.test.ts`（2 项）：仍期待旧升级/草稿路径。
3. `job-description-code.test.ts`（2 项）：测试输入包含已从严格 schema 移除的旧配置字段。
4. `resumes/__tests__/route-behavior.test.ts`：重新评估事务 mock 未实现 `onConflictDoNothing`，响应变为 500。

`git diff aee04c5c -- apps/server packages bun.lock` 为空；未扩展本轮范围修改这些失败。全量测试使用本机隔离 PostgreSQL（127.0.0.1:55439），未使用真实业务数据库执行测试写操作。

## 浏览器验收

- 招聘台：多选勾选时 URL 不变，取消不提交；点击应用才提交筛选并回第一页。
- 技能：100 项选项仅挂载约 16 项；搜索 TypeScript、方向键和回车选择、应用正常。实际滚动视口高度 256px，虚拟内容高度约 3208px。
- 人才库：选择“昨天”提交固定 `custom:2026-08-25:2026-08-25` 日期范围，保持分页边界稳定。
- 邮箱监听：保持“刷新 → 重置 → 立即轮询”顺序；日志起止日期有独立字段，日期边界与应用提交有交互回归测试。
- 表单题：默认显示“归档状态 是 未归档”；移除后 URL 为 `archivedFilter=all`，重置为 `archivedFilter=active`。
- 未修改主列表固定行高、日期吸顶和后台刷新实现。筛选栏改为搜索/动作与条件标签两行，骨架布局同步。

开发中类型检查的 i18n 编译与热更新曾触发 Vite SSR 初始化错误；干净启动开发进程后上述流程可正常验收。未把临时热更新状态当作功能通过证据。

## 原子条件与单行工具栏补充验收

本轮基线：`cba6f38b0d5ae2917f8618e3c5d63664ff9781a1`。本节覆盖后续追加的原子字段、按字段数自动选择模式、字段选择持久化，以及单行工具栏与仅清空值的要求；工具栏顺序与清空语义以本节为准。

- `bun run check`、8 个 workspace 类型检查通过。
- Web 全量：196 个文件、689 项测试通过。并发运行类型检查和两个端的测试时，两个模块导入测试超过 15 秒；单独以两个 worker 重跑 Web 全量通过，未提高测试超时或修改这些测试。
- Desktop 全量：43 个文件、163 项通过；补充招聘阶段进入 Filters 的回归用例。
- Shared 全量：47 个文件、471 项通过，包含原子字段白名单、非法输入、长度限制、AND 与字面匹配。
- 后端全量：1595 项通过、6 项失败；仍是上文列出的旧岗位断言/schema 输入与事务 mock 问题。补齐本地测试认证占位配置后，权限套件通过，不再因缺少认证环境失败。
- 本机隔离 PostgreSQL 搜索集成测试 28 项通过：两个简历表均覆盖公司/学校 AND、字段不串用、历史学校回退和 `%`/`_` 字面匹配。测试结束后关闭临时数据库，未修改业务数据库结构或回填数据。
- Python：ruff check、ruff format --check、146 项 pytest 通过；本轮未改 Python。

浏览器验收（本地开发服务，现有登录态，仅查询与界面操作）：

1. 人才库同时提交公司“字节”和学校“大学”，两个条件出现在同一 `textFilters` 参数中，正常返回列表。
2. 重新打开不带查询参数的人才库，保留公司/学校空标签，不恢复输入值；移除学校后再次打开也不会恢复它。单测进一步验证 `getOnInit` 在订阅前读取字段配置，存储中不含值。
3. 招聘台没有外置混合搜索框或阶段页签；字段选择器包含候选人、公司、学校、邮箱、电话、简历名、目标岗位、招聘阶段、创建人、技能、关联岗位、AI 评价。AI 面试阶段提交为 `stage=ai_interview`。
4. 部门页直接显示部门描述和部门名称两个输入框，没有 Filters 入口。
5. 邮箱监听使用 Filters。最终共享工具栏顺序为筛选、清空筛选、刷新、其他操作；立即轮询仍位于其他操作区。
6. 招聘台的条件控件、添加筛选、清空、刷新与创建按钮顶端均为同一坐标，已查看实际截图确认对齐。
7. 表单题默认“未归档”可清空为 `archivedFilter=all`，归档标签保留并显示空值提示，清空按钮变为禁用。交互测试覆盖从 URL/默认值产生、尚未存储的标签也能保留。
8. 清空在一次查询更新中回到第一页并清除旧勾选，不更改排序或当前队列。骨架按相同顺序单行排列；固定行高、日期分组吸顶及背景刷新实现不变。

## Standards

独立审查发现旧 `search` URL 可能成为隐藏条件，已修复为默认忽略；只有队列明确开启任务 ID 查找。复核后未发现新的阻断问题；字段白名单、SQL 字面匹配、权限约束及批量清空边界通过静态核验。

## Spec

独立需求审查复核了原子字段映射、分页/计数、字段键持久化、单行顺序和仅清空值。旧隐藏搜索问题已修复，最终限定范围无未解决的阻断项。审查未替代上面的自动测试与浏览器验收。
