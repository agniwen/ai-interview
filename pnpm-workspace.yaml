shellEmulator: true

trustPolicy: no-downgrade

# Known false positives — these packages were published before npm provenance
# became available, so their "trust downgrade" is expected and safe.
trustPolicyExclude:
  - "semver@6.3.1"

packages:
  - "packages/*"

ignoredBuiltDependencies:
  - msw
  - sharp
  - unrs-resolver
