import { MeetingSidebarSlots } from "@/components/features/meeting/meeting-sidebar-slots";

/**
 * Home-route sidebar content. 工作台 → 招聘台 (resume library).
 */
export function HomeSidebarSlots() {
  return <MeetingSidebarSlots />;
}
